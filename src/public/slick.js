$(document).ready(() => {
  $("#hot__slick").slick({
    autoplay: false,
    infinite: false,
    autoplaySpeed: 1000,
    adaptiveHeight: true,
    lazyLoad: "ondemand",
  });
  $("#hot-keys__slick").slick({
    autoplay: false,
    infinite: false,
    autoplaySpeed: 1000,
    adaptiveHeight: true,
    lazyLoad: "ondemand",
  });
});
